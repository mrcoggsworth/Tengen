apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "tengen.fullname" . }}-n8n-routes
  labels:
    {{- include "tengen.labels" . | nindent 4 }}
data:
  {{ base .Values.n8n.routesPath }}: |
    {{- .Values.n8n.routes | nindent 4 }}
