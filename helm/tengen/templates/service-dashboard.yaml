apiVersion: v1
kind: Service
metadata:
  name: {{ include "tengen.fullname" . }}-dashboard
  labels:
    {{- include "tengen.labels" . | nindent 4 }}
    app.kubernetes.io/component: dashboard
spec:
  type: ClusterIP
  ports:
    - port: {{ .Values.dashboard.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "tengen.componentLabels" (dict "root" . "component" "dashboard") | nindent 4 }}
